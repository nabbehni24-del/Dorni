"use client";
/* eslint-disable react-hooks/set-state-in-effect, @next/next/no-location-assign-relative-destination */
import { useCallback,useEffect,useMemo,useState } from "react";
import Image from "next/image";
import { ApiError, requestJson, errorMessage, copyText } from "@/lib/client-api";
import { Building2,Boxes,Check,Copy,Download,ExternalLink,LogOut,QrCode,RefreshCw,Send,ShieldCheck,Users,X } from "lucide-react";
import { DorniBrand } from "@/components/dorni-brand";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Badge } from "@/components/ui/badge";
type Batch={id:string;batch_code:string;quantity:number;generated_count:number;generation_status:string;distribution_status:string;created_at:string};type Code={id:string;batch_id:string;serial_number:string;public_token:string;ownership_state:string;activation_state:string};type Export={id:string;batch_id:string;expires_at:string;downloaded_at:string|null};type Overview={membership:{role:string};organization:{name:string;type:string;status:string;trusted_generation:boolean;generation_limit_per_day:number;registration_number:string|null};batches:Batch[];requests:{id:string;quantity:number;status:string;created_at:string}[];members:unknown[];codes:Code[];exports:Export[]};
export default function PartnerPage(){const [data,setData]=useState<Overview|null>(null),[quantity,setQuantity]=useState("10"),[notes,setNotes]=useState(""),[selected,setSelected]=useState<Code|null>(null),[error,setError]=useState(""),[notice,setNotice]=useState(""),[busy,setBusy]=useState(false);const load=useCallback(async()=>{try{const j=await requestJson<Overview>("/api/partner/overview");setData(j);setError("");}catch(e){if(e instanceof ApiError&&e.status===401){location.assign("/login");return;}setError(errorMessage(e));}},[]);useEffect(()=>{void load();const refresh=()=>{if(document.visibilityState==="visible")void load();};const timer=window.setInterval(refresh,15000);window.addEventListener("focus",refresh);window.addEventListener("online",refresh);return()=>{window.clearInterval(timer);window.removeEventListener("focus",refresh);window.removeEventListener("online",refresh);};},[load]);const claimed=useMemo(()=>data?.codes.filter(c=>c.ownership_state==="CLAIMED").length??0,[data]);async function post(url:string,payload:unknown){if(busy)return null;setBusy(true);setError("");setNotice("");try{const j=await requestJson<{batch?:{quantity:number;batchCode:string}}>(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});await load();return j;}catch(e){setError(errorMessage(e));return null;}finally{setBusy(false);}}async function action(){const qty=Number(quantity);const limit=data?.organization.trusted_generation?Math.min(1000,data.organization.generation_limit_per_day??100):100000;if(!Number.isInteger(qty)||qty<1||qty>limit){setError(`أدخل عدداً صحيحاً من 1 إلى ${limit}`);return;}if(data?.organization.trusted_generation){const j=await post("/api/partner/batches",{quantity:qty,productType:"STANDARD_CARD"});if(j?.batch)setNotice(`تم إصدار ${j.batch.quantity} أكواد في الدفعة ${j.batch.batchCode}. يمكنك عرضها وتنزيل ملف الدفعة.`);}else{const j=await post("/api/partner/batch-requests",{quantity:qty,productType:"STANDARD_CARD",notes});if(j)setNotice("تم حفظ الطلب وإرساله لعمليات دورني.");}}async function download(id:string){try{const j=await requestJson<{url:string}>(`/api/partner/exports/${id}`);window.location.assign(j.url);}catch(e){setError(errorMessage(e));}}async function logout(){try{await requestJson("/api/auth/logout",{method:"POST"});location.assign("/login");}catch(e){setError(errorMessage(e));}}async function copyLink(token:string){try{await copyText(`${location.origin}/t/${token}`);setNotice("تم نسخ رابط البطاقة");}catch(e){setError(errorMessage(e));}}if(!data&&!error)return <main className="loading-screen" dir="rtl"><RefreshCw className="spin"/> جاري تحميل بوابة الشركة</main>;return <main className="portal-page" dir="rtl"><header className="portal-header"><DorniBrand/><div className="portal-header-actions"><Badge><ShieldCheck/> بوابة الشركة</Badge><button onClick={logout} aria-label="تسجيل الخروج"><LogOut/></button></div></header><section className="portal-content">{error&&!data?<div className="empty-state"><Building2/><h1>تعذر فتح بوابة الشركة</h1><p role="alert">{error}</p><Button onClick={()=>void load()}>إعادة المحاولة</Button><a href="/login">رجوع للدخول</a></div>:<><div className="portal-title"><div><p className="eyebrow">{data!.organization.type}</p><h1>{data!.organization.name}</h1><p>صلاحيتك: {data!.membership.role} — بيانات الشركة محفوظة في حسابك</p></div><Badge>{data!.organization.status}</Badge></div>{error&&<p className="form-error">{error}</p>}{notice&&<p className="success-banner"><Check/> {notice}</p>}<div className="metric-grid"><article><Boxes/><b>{data!.codes.length}</b><span>أكواد صادرة</span></article><article><Check/><b>{claimed}</b><span>أكواد مفعّلة</span></article><article><Send/><b>{data!.requests.filter(r=>r.status==="PENDING").length}</b><span>طلبات معلقة</span></article><article><Users/><b>{data!.members.length}</b><span>أعضاء الشركة</span></article></div><div className="portal-grid"><article className="work-card"><h2>{data!.organization.trusted_generation?"إصدار دفعة أكواد وQR":"طلب دفعة جديدة"}</h2><p>{data!.organization.trusted_generation?`الأكواد تُنشأ وتُحفظ فوراً. حد الحساب ${data!.organization.generation_limit_per_day??100} كود يومياً.`:"يراجع فريق دورني الطلب ثم يصدر الدفعة مركزياً."}</p><Input aria-label="عدد الأكواد المطلوبة" type="number" step="1" min="1" max={data!.organization.trusted_generation?Math.min(1000,data!.organization.generation_limit_per_day??100):100000} value={quantity} onChange={e=>setQuantity(e.target.value)}/>{!data!.organization.trusted_generation&&<Input placeholder="ملاحظات اختيارية" value={notes} onChange={e=>setNotes(e.target.value)}/>}<Button onClick={action} disabled={busy||Number(quantity)<1}>{data!.organization.trusted_generation?<><QrCode/> إنشاء الأكواد وQR</>:<><Send/> إرسال الطلب</>}</Button></article><article className="work-card"><h2>ملفات الإنتاج الآمنة</h2><p>تحتوي الرقم التسلسلي ورمز التفعيل ورابط QR، ولا تظهر للعامة.</p>{data!.exports.length?data!.exports.map(x=><div className="list-row" key={x.id}><div><b>{new Date(x.expires_at).toLocaleDateString("ar-LY")}</b><p>{x.downloaded_at?"تم تنزيله سابقاً":"جاهز للتنزيل"}</p></div><Button variant="outline" onClick={()=>download(x.id)}><Download/> تنزيل CSV</Button></div>):<p>لا يوجد ملف إنتاج بعد.</p>}</article></div><article className="work-card"><h2>الأكواد وحالة التفعيل</h2><div className="code-table"><div className="code-table-head"><span>الرقم التسلسلي</span><span>الدفعة</span><span>الحالة</span><span>QR والمسح</span></div>{data!.codes.length?data!.codes.map(c=><div className="code-table-row" key={c.id}><b dir="ltr">{c.serial_number}</b><span dir="ltr">{data!.batches.find(b=>b.id===c.batch_id)?.batch_code??"—"}</span><Badge variant="outline">{c.ownership_state==="CLAIMED"?"مفعّل":"غير مفعّل"}</Badge><div className="qr-actions"><button onClick={()=>setSelected(c)}><QrCode/> عرض QR</button><button onClick={()=>void copyLink(c.public_token)}><Copy/> نسخ</button></div></div>):<p>ما فيش أكواد صادرة توا.</p>}</div></article></>}</section>{selected&&<div className="qr-dialog" role="dialog" aria-modal="true" aria-label="رمز QR للبطاقة"><button className="qr-dialog-backdrop" onClick={()=>setSelected(null)} aria-label="إغلاق"/><article><button className="qr-dialog-close" onClick={()=>setSelected(null)} aria-label="إغلاق"><X/></button><DorniBrand/><h2>رمز QR للبطاقة</h2><p dir="ltr">{selected.serial_number}</p><Image unoptimized width={420} height={420} src={`/api/partner/qr/${selected.id}`} alt={`QR ${selected.serial_number}`}/><small>عند المسح يفتح رابط البلاغ الخاص بهذا الكود. بعد ما الزبون يفعّله تتغير حالته هنا إلى مفعّل.</small><a href={`/t/${selected.public_token}`} target="_blank" rel="noreferrer"><ExternalLink/> تجربة رابط المسح</a></article></div>}</main>}
