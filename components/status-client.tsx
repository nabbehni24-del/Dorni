"use client";
import { useEffect, useState } from "react";
import { Check, Clock3, RefreshCw, ShieldCheck } from "lucide-react";
const responseLabel:Record<string,string>={ON_MY_WAY:"صاحب السيارة جاي",RESOLVED:"تم حل الموضوع",CANNOT_REACH_NOW:"صاحب السيارة مش قادر يوصل توا"};
export function StatusClient({token}:{token:string}){const [data,setData]=useState<{status:string;ownerResponse:string|null;updatedAt:number}|null>(null),[error,setError]=useState("");useEffect(()=>{let alive=true;async function load(){const r=await fetch(`/api/public/status/${token}`,{cache:"no-store"});const d=await r.json();if(!alive)return;if(!r.ok)setError(d.error);else setData(d);}void load();const timer=setInterval(load,5000);return()=>{alive=false;clearInterval(timer);};},[token]);return <main className="public-shell" dir="rtl"><section className="status-card">{error?<><div className="success-mark"><Clock3/></div><h1>رابط الحالة غير متاح</h1><p className="support-copy">{error}</p></>:!data?<><RefreshCw className="spin status-spinner"/><h1>جاري تحميل حالة البلاغ</h1></>:<><div className="success-mark"><Check/></div><p className="eyebrow">بلاغ محفوظ</p><h1>{data.ownerResponse?responseLabel[data.ownerResponse]:"تم إرسال التنبيه"}</h1><p className="support-copy">هذه الحالة تُحدّث تلقائياً من رد صاحب السيارة المحفوظ في دورني.</p><div className="status-steps"><div className="status-step is-done"><span>1</span><div><strong>تم استلام البلاغ</strong><small>مسجّل في النظام</small></div></div><div className={`status-step ${data.ownerResponse?"is-done":""}`}><span>2</span><div><strong>رد صاحب السيارة</strong><small>{data.ownerResponse?responseLabel[data.ownerResponse]:"في انتظار الرد"}</small></div></div></div><div className="privacy-note"><ShieldCheck/><span>الرابط خاص بهذا البلاغ وينتهي تلقائياً.</span></div></>}</section></main>}
