"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import { useCallback,useEffect,useRef,useState } from "react";
import { requestJson,errorMessage } from "@/lib/client-api";
import { AlertTriangle,CarFront,ChevronLeft,CircleAlert,DoorOpen,Lightbulb,LocateFixed,MoveRight,RefreshCw,ShieldCheck } from "lucide-react";
import { DorniBrand } from "./dorni-brand";
import { Button } from "./ui/button";
import { RadioGroup,RadioGroupItem } from "./ui/radio-group";
const reasons=[{value:"BLOCKING_EXIT",label:"السيارة تعيق خروجي",icon:MoveRight},{value:"PLEASE_MOVE",label:"الرجاء تحريك السيارة",icon:CarFront},{value:"LIGHTS_ON",label:"الأنوار ما زالت شغّالة",icon:Lightbulb},{value:"DOOR_OR_WINDOW_OPEN",label:"باب أو نافذة مفتوحة",icon:DoorOpen},{value:"VEHICLE_DAMAGE",label:"هناك ضرر أو مشكلة بالسيارة",icon:AlertTriangle},{value:"URGENT_ATTENTION",label:"تنبيه عاجل",icon:CircleAlert}];
export function PublicReportClient({token}:{token:string}){const [vehicle,setVehicle]=useState<{manufacturer:string;model:string;color:string}|null>(null),[reason,setReason]=useState("BLOCKING_EXIT"),[location,setLocation]=useState<{latitude:number;longitude:number}|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);const session=useRef<string|null>(null);const sending=useRef(false);const load=useCallback(async()=>{setError("");try{const d=await requestJson<{vehicle:{manufacturer:string;model:string;color:string}}>(`/api/public/codes/${token}`);setVehicle(d.vehicle);}catch(e){setError(errorMessage(e));}},[token]);useEffect(()=>{void load();},[load]);function requestLocation(){if(!navigator.geolocation)return setError("الموقع غير مدعوم في هذا الجهاز");navigator.geolocation.getCurrentPosition(p=>{setLocation({latitude:p.coords.latitude,longitude:p.coords.longitude});setError("");},()=>setError("ما قدرناش ناخذ الموقع، تقدر ترسل البلاغ بدونه"),{enableHighAccuracy:false,timeout:8000,maximumAge:60000});}async function submit(){if(sending.current)return;sending.current=true;setBusy(true);setError("");try{if(!session.current){try{session.current=localStorage.getItem("dorni_scanner_session");}catch{/* Private browsing can restrict storage. */}if(!session.current){const bytes=new Uint8Array(24);crypto.getRandomValues(bytes);session.current=Array.from(bytes,b=>b.toString(16).padStart(2,"0")).join("");try{localStorage.setItem("dorni_scanner_session",session.current);}catch{/* In-memory session remains usable. */}}}const d=await requestJson<{statusPath:string}>("/api/public/reports",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({publicToken:token,reportType:reason,scannerSessionToken:session.current,...location})});if(!new RegExp("^/status/[A-Za-z0-9_-]+$").test(d.statusPath))throw new Error("تم إرسال البلاغ لكن تعذر فتح المتابعة.");window.location.assign(d.statusPath);}catch(e){setError(errorMessage(e));}finally{sending.current=false;setBusy(false);}}return <main className="public-shell" dir="rtl"><header className="public-header"><DorniBrand/><span className="secure-chip"><ShieldCheck/> تواصل آمن</span></header><section className="scan-card">{!vehicle&&!error?<div className="loading-inline"><RefreshCw className="spin"/> جاري التحقق من الكود...</div>:error&&!vehicle?<div className="invalid-code"><CircleAlert/><h1>تعذر عرض البطاقة</h1><p role="alert">{error}</p><Button onClick={()=>void load()}>إعادة المحاولة</Button></div>:<><div className="vehicle-summary"><div className="vehicle-icon"><CarFront/></div><div><p className="eyebrow">تأكيد السيارة</p><h1>{vehicle!.manufacturer} {vehicle!.model}</h1><p>{vehicle!.color}</p></div><span className="verified-badge">بطاقة مفعّلة</span></div><div className="divider"/><div className="report-heading"><span className="step-number">1</span><div><h2>شن تبي تقول لصاحب السيارة؟</h2><p>اختار سبب واضح؛ ما فيش رسائل حرة أو كشف بيانات.</p></div></div><RadioGroup aria-label="سبب التنبيه" disabled={busy} value={reason} onValueChange={setReason} className="reason-grid">{reasons.map(({value,label,icon:Icon})=><label key={value} className={`reason-option ${reason===value?"is-selected":""}`}><RadioGroupItem value={value} className="sr-only"/><span className="reason-icon"><Icon/></span><span>{label}</span><span className="selection-dot"/></label>)}</RadioGroup><Button type="button" variant="outline" disabled={busy} onClick={requestLocation}><LocateFixed/> {location?"تمت إضافة الموقع اختيارياً":"إضافة موقعي اختيارياً"}</Button>{error&&<p role="alert" className="form-error">{error}</p>}<Button className="primary-action report-submit" onClick={submit} disabled={busy}>{busy?"جاري إرسال التنبيه...":"إرسال التنبيه"} <ChevronLeft/></Button><p className="privacy-footer"><ShieldCheck/> لا يظهر اسم أو رقم صاحب السيارة، وإضافة الموقع اختيارية.</p></>}</section></main>}
